<!DOCTYPE html>
<html lang="pl">

<head>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #F2F2F2;
            margin: 0;
            padding: 20px;
            color: #242424;
        }

        .container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            background-color: #FFFFFF;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
        }

        h2 {
            color: #FE8D1C;
            text-align: center;
            font-size: 24px;
        }

        p {
            font-size: 16px;
            line-height: 1.6;
            color: #555;
        }

        .order-info {
            background-color: #f9f9f9;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }

        footer {
            text-align: center;
            font-size: 14px;
            color: #888;
            margin-top: 20px;
        }

        .auto-notice {
            font-size: 12px;
            color: #999;
            text-align: center;
            margin-top: 30px;
            font-style: italic;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>Nowe zamówienie Twojego produktu</h2>

        <div class="order-info">
            <p><strong>Produkt:</strong>
                <%= order.product.name %>
            </p>
            <p><strong>Numer zamówienia:</strong>
                <%= order.orderNumber %>
            </p>
            <p><strong>Kupujący:</strong>
                <%= order.user.firstName %>
                    <%= order.user.lastName %> (<%= order.user.email %>)
            </p>
            <p><strong>Adres wysyłki:</strong>
                <%= order.shippingAddress.street %>
                    <%= order.shippingAddress.houseNumber %>, <%= order.shippingAddress.postalCode %>
                            <%= order.shippingAddress.city %>
            </p>
        </div>

        <p style="text-align: center; font-weight: bold;">Masz 7 dni na nadanie paczki – w przeciwnym razie zamówienie
            zostanie anulowane.</p>

        <div class="auto-notice">
            <p>Ta wiadomość została wygenerowana automatycznie. Prosimy na nią nie odpowiadać.</p>
        </div>

        <footer>
            <p>Vendoro &copy; 2025</p>
        </footer>
    </div>
</body>

</html>